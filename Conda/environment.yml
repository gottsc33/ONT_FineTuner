name: ont-finetuner

channels:
  - pytorch
  - nvidia
  - conda-forge
  - bioconda

dependencies:
  - python=3.10
  - pip
  - numpy
  - minimap2=2.28

  # GPU-enabled PyTorch. Change the CUDA version if required by the system.
  - pytorch
  - pytorch-cuda=11.8

  - pip:
      - ont-bonito==0.8.1